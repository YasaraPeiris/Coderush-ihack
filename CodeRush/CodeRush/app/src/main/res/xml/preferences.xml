<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!--<CheckBoxPreference
        android:key="pref_sync"
        android:title="Activate"
        android:summary="Activate I'm SAFE"
        android:defaultValue="true" />-->
    <ListPreference
        android:key="timeout"
        android:title="Set Timeout"
        android:summary="Set timeout to send alerts"
        android:dialogTitle="Set timeout to send alerts"/>
        <!--android:entries="@array/list"
        android:entryValues="@array/list_values"
        android:defaultValue="10"-->
    <ListPreference
        android:key="passcode_true"
        android:title="My Correct Passcode"
        android:summary="Set passcode to cancel alerts"/>

</PreferenceScreen>